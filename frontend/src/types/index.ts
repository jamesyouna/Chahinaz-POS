export type Role='ADMIN'|'MANAGER'|'TELLER';export type SaleStatus='OPEN'|'HELD'|'COMPLETED'|'VOIDED';export type Money=number;
export interface User{username:string;role:Role}export interface Category{id:string;name:string;description?:string;active:boolean}export interface Product{id:string;sku:string;barcode?:string;name:string;description?:string;categoryId:string;categoryName:string;sellingPriceUsd:Money;costPriceUsd?:Money;stockQuantity?:number;lowStockThreshold?:number;imageUrl?:string;inStock?:boolean;active?:boolean;publicationStatus?:string;featured?:boolean;newArrival?:boolean}
export interface SaleItem{id:string;productId:string;sku:string;name:string;originalUnitPriceUsd:Money;unitPriceUsd:Money;discountUsd:Money;quantity:number;lineTotalUsd:Money;overrideReason?:string}
export interface Payment{type:string;amountUsd?:Money;amountLbp?:Money;manuallyConfirmed:boolean}export interface ReturnItem{saleItemId:string;quantity:number;amountUsd:number;restockable:boolean}export interface SaleReturn{id:string;refundUsd?:number;refundLbp?:number;method:string;reason:string;createdAt:string;items:ReturnItem[]}export interface Sale{id:string;saleNumber:string;status:SaleStatus;createdAt:string;completedAt?:string;cashier:string;items:SaleItem[];subtotalUsd:Money;totalDiscountUsd:Money;totalUsd:Money;exchangeRate?:Money;payments:Payment[];changeUsd:Money;changeLbp:Money;note?:string;returns:SaleReturn[]}
export interface Register{id:string;employeeName:string;status:'OPEN'|'CLOSED';openedAt:string;closedAt?:string;openingCashUsd:Money;openingCashLbp:Money;expectedCashUsd?:Money;expectedCashLbp?:Money;actualCashUsd?:Money;actualCashLbp?:Money;differenceUsd?:Money;differenceLbp?:Money}
export interface Page<T>{content:T[];page:number;size:number;totalElements:number;totalPages:number}export interface Dashboard{grossSales:Money;netSales:Money;completedTransactions:number;averageTransactionValue:Money;totalDiscounts:Money;totalRefunds:Money;grossProfit:Money;grossMarginPercent?:Money;lowStockProducts:number;outOfStockProducts:number}
